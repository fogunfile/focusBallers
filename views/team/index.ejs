<%- include("../partials/header") -%>
    <header class="jumbotron">
        <h1>All Teams</h1>
        <a href="/team/new" class="btn btn-primary">Create a New Team</a>
    </header>
    <div class="container">
        <ol>
            <% teams.forEach((team)=> { %>
                <h2>
                    <li>
                        <%= team.name %><a href="/team/<%= team._id %>/edit" class="btn btn-md btn-outline-primary mx-3">Edit</a>
                    </li>
                </h2>
            <% }) %>
        </ol>
    </div>

<%- include("../partials/footer") -%>
